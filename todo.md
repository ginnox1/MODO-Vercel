[x] Navigation bar must stay, or appear on scroll up move.
[x] Make sure the mobile view of the hero page shows the entire image body.
[x] When hero-before.jpg image is fully revealed, the tag 'Unfolds as Workspace' should disappear.
[x] Add dark/light theme
[x] change favicon modo_logo.png in /client/public.
[x] Revamp the admin page to show charts of analytics. Also as suggested in previous session, add the approve button for submissions including sending confirmation to the user.
[x] Mobile drag flickers on mobile. Not smooth at all. The drag bar only moves in steps. Too bad. desktop experience is cool. Please fix.

[x] On the landing page hero (the bed/desk comparison image), add a small, unobtrusive
label identifying it as a concept visualization rather than a real photo — the desk is a real render of our actual 3D model, but the surrounding room is AI-composited,
not a photographed space. 

Style it consistently with the existing small-caps pill badges already on the image
(e.g. "FOLDABLE & SPACE-SAVING"). Place it in a corner that doesn't compete with the
existing badges — bottom-right works if the "DRAG TO COMPARE" pill is bottom-center.

Label text: "CONCEPT VISUALIZATION" or "DESIGN RENDER" (pick whichever matches our
existing badge tone better).

Keep it small and low-contrast enough not to undercut the hero's impact — this is an
honesty/trust detail, not a headline.

